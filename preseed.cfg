# Automated Debian 13 installation for the syn-sender VM.
# Password fields are intentionally absent so Debian asks the user.

### Language, keyboard, and time
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i clock-setup/utc boolean true
d-i time/zone string Asia/Dhaka
d-i clock-setup/ntp boolean true

### Temporary installation network (libvirt default NAT/DHCP)
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string syn-sender
d-i netcfg/get_domain string

### Debian package mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Accounts
# Disable direct root login. The normal user receives sudo access.
d-i passwd/root-login boolean false
d-i passwd/make-user boolean true
d-i passwd/user-fullname string SYN Sender
d-i passwd/username string sender
# passwd/user-password and passwd/user-password-again are deliberately omitted.
# The installer must ask the user to enter and confirm this password.

### Partition only the sender VM's virtual disk
d-i partman-auto/disk string /dev/vda
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Packages: no graphical desktop
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string sudo python3 iproute2 iputils-ping tcpdump
d-i pkgsel/upgrade select safe-upgrade
popularity-contest popularity-contest/participate boolean false

### Boot loader and completion
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default
d-i finish-install/reboot_in_progress note

### Give the normal account administrative access through sudo
d-i preseed/late_command string in-target /usr/sbin/usermod -aG sudo sender
